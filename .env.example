# LLM Configuration
# Required for AI enrichment features (skills densification, entity enrichment)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# LLM model selection (optional - defaults to gpt-4o-mini)
# Available models: gpt-4o-mini, gpt-4o, gpt-3.5-turbo
LLM_MODEL=gpt-4o-mini

# Neo4j Configuration
# Required for graph ingestion and enrichment features
# Default values for local development
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-neo4j-password-here

# API Configuration (optional - defaults shown)
# HARMONIZER_API_URL=http://127.0.0.1:8000
# ENTITY_RESOLVER_API_URL=http://127.0.0.1:8001

# CORS Configuration
# For production, specify allowed origins (comma-separated)
# Example: CORS_ORIGINS=https://app.example.com,https://admin.example.com
# Default for development: http://localhost:3000,http://127.0.0.1:8000
# Leave unset for development defaults
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:8000

# Environment (optional)
# NODE_ENV=development